🧹 Improve code health by implementing missing Overdrive feature

🎯 **What:** Hooked up the missing 'Overdrive' (supercharging) action in the key mapper and implemented its logic in state.
💡 **Why:** The feature was marked as an unimplemented comment in `main.rs` despite being listed in the UI controls and having underlying state support (`is_supercharging`). This improves code health by removing dead code/unimplemented placeholders and completing the intended behavior loop.
✅ **Verification:** Verified key mapping with `KeyCode::Char('o')` matches. Ensured the state toggles and sets appropriate feedback. Ran cargo fmt, clippy, and cargo test.
✨ **Result:** Users can now actually toggle Overdrive using 'o' or 'O' and receive feedback in the system log.
